# Copy to keystore.properties and fill in real values.
# Never commit keystore.properties or *.keystore files.

storeFile=keystore/video-compressor-release.keystore
storePassword=REPLACE_ME
keyAlias=video-compressor
keyPassword=REPLACE_ME
